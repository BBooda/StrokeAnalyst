function create_ml_features()
    % create ml_feature vector, this vector consist of 3 pixel intensity
    % features for every color channel. 2 features for the zscore image and
    % 2 features for the zscore image difference

end