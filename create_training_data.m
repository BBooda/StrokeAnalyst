% create data for machine learning training.
% actions to do: create zscore features ( zscore, zscore hemisphere
% difference), transform data to subject space, use only affected
% hemisphere.

% perform registration to average slice
function training_data = create_training_data(subject_info)
    % aquire data 
    

end